import { StyleSheet } from 'react-native';
import { container } from '../../themes/application.styles.js';

export default StyleSheet.create({
  container: {
    ...container,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
